import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.weeklyschedule.app',
  appName: 'Weekly Schedule Manager',
  webDir: 'dist'
};

export default config;
